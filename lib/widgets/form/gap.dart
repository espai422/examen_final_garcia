import 'package:flutter/material.dart';

/// Widget to show a gap between widgets
class Gap extends StatelessWidget {
  const Gap({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
